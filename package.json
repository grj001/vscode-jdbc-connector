{
  "name": "vscode-jdbc-connector",
  "displayName": "JDBC Connector",
  "description": "A VSCode extension for connecting to databases via JDBC",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:jdbcConnections",
    "onView:jdbcQueryResultView",
    "onCommand:vscode-jdbc-connector.newQuery",
    "onCommand:vscode-jdbc-connector.executeQuery",
    "onCommand:vscode-jdbc-connector.executeSelectedQuery"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "jdbc-connector",
          "title": "JDBC Connector",
          "icon": "resources/database.svg"
        }
      ],
      "panel": [
        {
          "id": "jdbcQueryPanel",
          "title": "JDBC 查询结果",
          "icon": "resources/database.svg"
        }
      ]
    },
    "views": {
      "jdbc-connector": [
        {
          "id": "jdbcConnections",
          "name": "数据库连接",
          "visibility": "visible"
        }
      ],
      "jdbcQueryPanel": [
        {
          "id": "jdbcQueryResultView",
          "name": "查询结果",
          "visibility": "visible",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "vscode-jdbc-connector.newConnection",
        "title": "新建连接",
        "icon": "$(add)"
      },
      {
        "command": "vscode-jdbc-connector.refreshConnections",
        "title": "刷新",
        "icon": "$(refresh)"
      },
      {
        "command": "vscode-jdbc-connector.editConnection",
        "title": "编辑连接",
        "icon": "$(edit)"
      },
      {
        "command": "vscode-jdbc-connector.newQuery",
        "title": "新建查询",
        "icon": "$(file-code)"
      },
      {
        "command": "vscode-jdbc-connector.executeQuery",
        "title": "执行查询",
        "icon": "$(play)"
      },
      {
        "command": "vscode-jdbc-connector.executeSelectedQuery",
        "title": "运行已选中",
        "icon": "$(symbol-event)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "vscode-jdbc-connector.executeQuery",
          "when": "resourceExtname == .sql",
          "group": "navigation@1"
        },
        {
          "command": "vscode-jdbc-connector.executeSelectedQuery",
          "when": "resourceExtname == .sql",
          "group": "navigation@2"
        }
      ],
      "view/title": [
        {
          "command": "vscode-jdbc-connector.newConnection",
          "when": "view == jdbcConnections",
          "group": "navigation"
        },
        {
          "command": "vscode-jdbc-connector.refreshConnections",
          "when": "view == jdbcConnections",
          "group": "navigation"
        },
        {
          "command": "vscode-jdbc-connector.editConnection",
          "when": "view == jdbcConnections",
          "group": "navigation"
        },
        {
          "command": "vscode-jdbc-connector.newQuery",
          "when": "view == jdbcConnections",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "18.x",
    "typescript": "^5.3.3"
  }
}
